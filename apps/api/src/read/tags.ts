/**
 * Tag/collection score surface — aggregate asset & address scores per tag (the derivative "tag
 * scores" layer: collection scoreboards, cohort stats). Stub: endpoints land with the tag-scores wave.
 */
import { router } from "./respond";

export const tags = router();
