/**
 * Graph-reputation read surface (Phase C) — the standalone Min-k-PPR trust trait.
 * See docs/graph-reputation.md. Stub: endpoints land with the graph-trait wave.
 */
import { router } from "./respond";

export const graph = router();
